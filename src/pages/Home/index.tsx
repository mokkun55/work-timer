import { Form } from "@/features/input/components/form";

export const Home = (): React.ReactNode => {
  return (
    <div>
      {/* 現在の進捗 */}

      {/* 記録フォーム */}
      <Form />

      {/* 最近の記録一覧 */}

      {/* 設定画面へ */}
    </div>
  );
};
